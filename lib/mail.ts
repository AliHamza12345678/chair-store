/**
 * All email sending lives in lib/email.ts (this file was an empty
 * duplicate). Kept as a re-export for backward compatibility — new code
 * should import from "@/lib/email" directly.
 */
export * from "@/lib/email"
